% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target_weighted.R
\name{evaluate_tcm_target_sets_by_ppi}
\alias{evaluate_tcm_target_sets_by_ppi}
\title{Evaluate target-set proximity to a weighted disease module on a PPI network}
\usage{
evaluate_tcm_target_sets_by_ppi(
  target_sets,
  disease_targets,
  ppi = NULL,
  ppi_path = NULL,
  set_col = NULL,
  target_col = "target",
  disease_gene_col = "symbol",
  disease_weight_col = "disease_weight",
  weight_attr = "score",
  distance_attr = "distance",
  n_perm = 1000L,
  degree_bins = 20L,
  seed = 42L,
  warn_coverage = 0.5
)
}
\arguments{
\item{target_sets}{A character vector, a target data frame, or a named list
of character vectors/data frames. When a data frame contains multiple
groups, use \code{set_col} to split it into target sets.}

\item{disease_targets}{A scored disease target data frame or the output of
\code{\link{prepare_disease_weights}}.}

\item{ppi}{An undirected \code{igraph} PPI background network. If
\code{NULL}, the package's built-in STRING background is used.}

\item{ppi_path}{Optional path to a saved igraph RDS used when \code{ppi} is
\code{NULL}.}

\item{set_col}{Optional grouping column in \code{target_sets}.}

\item{target_col}{Target column in data-frame target-set inputs.}

\item{disease_gene_col}{Gene column in \code{disease_targets}.}

\item{disease_weight_col}{Disease weight column in \code{disease_targets}.}

\item{weight_attr}{Edge confidence attribute. Used to derive distances when
\code{distance_attr} is absent.}

\item{distance_attr}{Edge distance attribute. Defaults to \code{"distance"}.}

\item{n_perm}{Number of degree-matched random target sets per observed set.}

\item{degree_bins}{Number of degree quantile bins for background matching.}

\item{seed}{Random seed.}

\item{warn_coverage}{Numeric coverage threshold below which a warning is
issued for target-set or disease targets present in the PPI network.}
}
\value{
A \code{tcm_ppi_set_eval} object containing \code{result},
\code{set_details}, \code{disease_module}, \code{diagnostics},
\code{ppi_summary}, and \code{params}.
}
\description{
This set-level analysis is closer to the original TCMNet question than
single-target ranking: does a herb, formula, compound group, or any target
set lie closer to the disease module than expected from degree-matched random
target sets?
}
