% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pubmed.R
\name{get_pubmed_table}
\alias{get_pubmed_table}
\title{Extract and Sort PubMed Literature Table}
\usage{
get_pubmed_table(x, n = NULL, file = NULL)
}
\arguments{
\item{x}{An object of class 'tcm_pubmed' generated by \code{get_pubmed_data}.}

\item{n}{Integer. Number of top records to return. If \code{NULL} (default), returns all records.}

\item{file}{Character. Optional file path (e.g., "results.csv"). If provided, the table will be saved to this location.}
}
\value{
A data frame containing the sorted literature records.
}
\description{
Accesses the literature data within a 'tcm_pubmed' object and returns it as
a data frame sorted by publication year in descending order.
}
\examples{
\dontrun{
## get result from pubmed
result <- get_pubmed_data(tcm_name = "Artemisinin", 
disease_name = "Malaria", 
year_range = c(2020, 2025),
retmax = 100,
email = "your email")

# Get the sorted table
lit_table <- get_pubmed_table(result)

# Get top 10 records and save to CSV
lit_table <- get_pubmed_table(result, n = 10, file = "top10_lit.csv")
}

}
