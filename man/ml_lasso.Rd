% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_models.r
\name{ml_lasso}
\alias{ml_lasso}
\title{LASSO feature selection (a wrapper function from \code{\link[=ml_enet]{ml_enet()}})
Calls \code{\link[=ml_enet]{ml_enet()}} with \code{alpha = 1}.}
\usage{
ml_lasso(
  ml_data,
  lambda_rule = c("1se", "min"),
  cv_folds = 10,
  seed = 2025,
  ...,
  balance_method = c("none", "auto", "weights", "down")
)
}
\arguments{
\item{ml_data}{A \code{tcm_ml_data} object from \code{\link[=prepare_ml_data]{prepare_ml_data()}}.}

\item{lambda_rule}{\code{"1se"} (parsimonious) or \code{"min"} (best AUC).}

\item{cv_folds}{Number of CV folds. Default 10.}

\item{seed}{Random seed. Default 2025.}

\item{...}{Passed to \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}.}

\item{balance_method}{Class-balancing strategy. The default
\code{"none"} preserves the original workflow; \code{"auto"} uses
inverse-frequency weights only when imbalance is detected.}
}
\value{
A \code{tcm_ml} object (method = "lasso").
}
\description{
LASSO feature selection (a wrapper function from \code{\link[=ml_enet]{ml_enet()}})
Calls \code{\link[=ml_enet]{ml_enet()}} with \code{alpha = 1}.
}
\examples{
\dontrun{
  ml_data <- prepare_ml_data(expr_mat, group)
  res <- ml_lasso(ml_data)
}
}
