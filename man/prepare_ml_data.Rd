% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_utils.R
\name{prepare_ml_data}
\alias{prepare_ml_data}
\title{Prepare expression data for ML feature selection}
\usage{
prepare_ml_data(
  expr_mat,
  group,
  positive_class = NULL,
  genes = NULL,
  split = FALSE,
  train_ratio = 0.7,
  train_idx = NULL,
  test_expr = NULL,
  test_group = NULL,
  seed = 2025,
  imbalance_threshold = 0.25,
  imbalance_action = c("warn", "message", "none")
)
}
\arguments{
\item{expr_mat}{Numeric matrix (genes x samples). Row names = gene symbols.}

\item{group}{Factor/character of length \code{ncol(expr_mat)}, two levels.}

\item{positive_class}{Which level to treat as the positive class (\code{levels[[1]]}).
Default \code{NULL}: if \code{group} is already an ordered factor, its level order is
preserved; otherwise levels are sorted alphabetically.}

\item{genes}{Optional character vector of candidate genes to keep. Default is NULL.}

\item{split}{Logical. \code{TRUE} = Mode B. Default is \code{FALSE}.}

\item{train_ratio}{Fraction for training when \code{split = TRUE}. Default is 0.7.}

\item{train_idx}{Optional integer indices overriding \code{train_ratio}.}

\item{test_expr}{External validation matrix (genes x samples) for Mode C.}

\item{test_group}{Labels for \code{test_expr} (required if \code{test_expr} given).}

\item{seed}{Random seed. Default is 2025.}

\item{imbalance_threshold}{Minority-class fraction below which the training
data are flagged as imbalanced. Default is 0.25.}

\item{imbalance_action}{How to report detected imbalance: a warning, a
message, or no condition.}
}
\value{
A \code{tcm_ml_data} list containing the original train/test fields plus
training and optional test class-balance summaries.
}
\description{
Supports three modes:
\itemize{
\item \strong{Mode A} (default): full-data CV, no hold-out (\code{split = FALSE}).
\item \strong{Mode B}: internal train/test split (\code{split = TRUE}).
\item \strong{Mode C}: external validation (\code{test_expr} + \code{test_group}).
}
}
\examples{
\dontrun{
  ## Mode A: full CV
  ml_data <- prepare_ml_data(expr_mat, group, positive_class = "Disease")
  ## Mode B: internal split
  ml_data <- prepare_ml_data(expr_mat, group, split = TRUE, train_ratio = 0.7)
}
}
