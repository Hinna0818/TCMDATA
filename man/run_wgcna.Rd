% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/WGCNA.R
\name{run_wgcna}
\alias{run_wgcna}
\title{Run weighted gene co-expression network analysis}
\usage{
run_wgcna(
  expr_mat,
  traits = NULL,
  power = NULL,
  powers = c(1:10, seq(12, 20, by = 2)),
  scale_free_R2 = 0.85,
  top_n = NULL,
  network_type = c("unsigned", "signed"),
  tom_type = NULL,
  cor_type = c("pearson", "bicor"),
  max_block_size = 5000,
  min_module_size = 30,
  merge_cut_height = 0.25,
  deep_split = 2,
  reassign_threshold = 0,
  pam_respects_dendro = FALSE,
  seed = 2025,
  n_threads = 1,
  verbose = 1
)
}
\arguments{
\item{expr_mat}{Numeric expression matrix with genes in rows and samples in
columns.}

\item{traits}{Optional sample-level vector, factor, matrix, or data frame.
Rows should correspond to samples; row names are used for matching when
available. Categorical variables are converted to indicator variables.}

\item{power}{Positive soft-thresholding power. If \code{NULL}, it is selected with
\code{\link[WGCNA:pickSoftThreshold]{WGCNA::pickSoftThreshold()}}.}

\item{powers}{Candidate powers used when \code{power = NULL}.}

\item{scale_free_R2}{Target scale-free topology fit used for power selection.}

\item{top_n}{Optional number of most variable genes to retain before network
construction.}

\item{network_type}{Network type, either \code{"unsigned"} or \code{"signed"}.}

\item{tom_type}{Topological-overlap type. Defaults to \code{network_type}.}

\item{cor_type}{Correlation method, either \code{"pearson"} or \code{"bicor"}.}

\item{max_block_size}{Maximum number of genes per block.}

\item{min_module_size}{Minimum module size.}

\item{merge_cut_height}{Height used to merge similar modules.}

\item{deep_split}{Module-detection sensitivity passed to
\code{\link[WGCNA:blockwiseModules]{WGCNA::blockwiseModules()}}.}

\item{reassign_threshold}{P-value threshold for reassigning genes between
modules.}

\item{pam_respects_dendro}{Whether PAM assignments must respect the
dendrogram.}

\item{seed}{Random seed used by blockwise module detection.}

\item{n_threads}{Number of WGCNA worker threads. Use \code{1} for deterministic
serial execution.}

\item{verbose}{WGCNA verbosity level.}
}
\value{
A \code{tcm_wgcna} object containing the selected power, network,
module assignments, eigengenes, gene-level module membership, module gene
lists, and optional module-trait and gene-trait correlations.
}
\description{
A lightweight wrapper around \code{\link[WGCNA:pickSoftThreshold]{WGCNA::pickSoftThreshold()}} and
\code{\link[WGCNA:blockwiseModules]{WGCNA::blockwiseModules()}}. Expression data use the same orientation as
other TCMDATA workflows: genes in rows and samples in columns.
}
\examples{
\dontrun{
set.seed(1)
expr <- matrix(rnorm(80 * 20), nrow = 80)
rownames(expr) <- paste0("Gene", seq_len(nrow(expr)))
colnames(expr) <- paste0("Sample", seq_len(ncol(expr)))
group <- factor(rep(c("Control", "Disease"), each = 10))

fit <- run_wgcna(
  expr,
  traits = group,
  power = 6,
  min_module_size = 10
)
fit$gene_info
}

}
