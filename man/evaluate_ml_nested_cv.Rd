% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_nested_cv.R
\name{evaluate_ml_nested_cv}
\alias{evaluate_ml_nested_cv}
\title{Evaluate ML feature selection with nested cross-validation}
\usage{
evaluate_ml_nested_cv(
  ml_data,
  methods = c("lasso", "rf", "svm_rfe", "xgboost"),
  outer_folds = 5L,
  outer_repeats = 1L,
  inner_folds = 5L,
  inner_repeats = 1L,
  balance_method = c("none", "auto", "weights", "down"),
  metric = "ROC",
  seed = 2025L,
  keep_fold_models = FALSE,
  model_args = list()
)
}
\arguments{
\item{ml_data}{A \code{tcm_ml_data} object from \code{\link[=prepare_ml_data]{prepare_ml_data()}}.}

\item{methods}{Models to evaluate: \code{"lasso"}, \code{"rf"}, \code{"svm_rfe"},
and/or \code{"xgboost"}.}

\item{outer_folds}{Number of stratified outer folds.}

\item{outer_repeats}{Number of repeated outer fold partitions.}

\item{inner_folds}{Number of inner folds passed to model functions.}

\item{inner_repeats}{Number of inner repeats used by SVM-RFE.}

\item{balance_method}{Class-balancing strategy passed to each model.}

\item{metric}{Optimization metric passed to SVM-RFE. Nested performance is
always summarized with ROC AUC.}

\item{seed}{Random seed.}

\item{keep_fold_models}{Whether to retain fitted models from every outer fold.}

\item{model_args}{Optional named list of additional arguments for each model,
for example \code{list(rf = list(n_trees = 300))}. Fold-control arguments
are managed by this function and cannot be overridden here.}
}
\value{
A \code{tcm_nested_cv} object containing patient-level performance,
outer-fold predictions, feature stability, fold summaries, optional fold
models, parameters, and auditable fold indices.
}
\description{
Runs an outer stratified cross-validation loop around the existing TCMDATA
model functions. Feature filtering, class balancing, tuning, and feature
selection are performed using only the outer training fold. Existing Mode B
or Mode C test data in \code{ml_data} remain isolated.
}
\examples{
\dontrun{
nested <- evaluate_ml_nested_cv(
  ml_data,
  methods = c("lasso", "rf"),
  outer_folds = 5,
  inner_folds = 5
)
nested$performance
}

}
